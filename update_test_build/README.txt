Local self-update test files (not stored in git).

Regenerate after code changes:

    python tools/build_update_test_build.py

Then run:

    update_test_build\Copasta\Copasta.exe

Keep Copasta.zip and Copasta.zip.sha256 in this folder for the local update test.
